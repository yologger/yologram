name: yologram-web-v1

on:
  push:
    branches: [master]
    paths:
      - 'v1/yologram-web-v1/**'
  pull_request:
    branches: [master]
    paths:
      - 'v1/yologram-web-v1/**'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
