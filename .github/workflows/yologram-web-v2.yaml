name: yologram-web-v2

on:
  push:
    branches: [master]
    paths:
      - 'yologram-web-v2/**'
  pull_request:
    branches: [master]
    paths:
      - 'yologram-web-v2/**'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
