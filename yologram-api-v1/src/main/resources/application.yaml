server:
  port: 5000

spring:
  application:
    name: yologram-api-v1
  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration
      - org.springframework.boot.autoconfigure.data.redis.RedisAutoConfiguration
      - org.springframework.boot.autoconfigure.data.redis.RedisRepositoriesAutoConfiguration

cache:
  data:
    redis:
      host:
      port: 6379

# 이벤트 스트림(Kinesis) — 스트림 이름은 비밀값이 아니라 고정 이름이므로 Parameter Store가 아닌 yaml에 직접 둔다.
# 기본값은 빈 값 = 발행 스킵 (로컬·테스트에서 prod 스트림이 오염되지 않도록). prod는 application-prod.yaml에서 지정
event:
  stream:
    post-view:
      name:

database:
  main:
    writer:
      datasource:
        driver-class-name: com.mysql.cj.jdbc.Driver
        # 커넥션 수명 15분 — 낡은 커넥션 주기 교체 (번장 bun-ums-api 미러, Hikari 기본 30분 단축).
        # 죽은 커넥션 소켓 타임아웃은 JDBC URL의 socketTimeout/connectTimeout이 담당 (SSM url 파라미터)
        hikari:
          max-lifetime: 900000
    reader:
      datasource:
        driver-class-name: com.mysql.cj.jdbc.Driver
        hikari:
          max-lifetime: 900000
    jpa:
      properties:
        hibernate.jdbc.batch_size: 50
        hibernate.order_inserts: true
        hibernate.order_updates: true
        hibernate.default_batch_fetch_size: 100
        hibernate.jdbc.time_zone: Asia/Seoul
        hibernate.cache.use_second_level_cache: false
        hibernate.cache.use_query_cache: false
        hibernate.hbm2ddl.auto: validate
      show-sql: false
      open-in-view: false

yologram:
  ses:
    from-address: no-reply@yologram.link
  auth:
    jwt:
      secret:   # /yologram/service/yologram-api-v1_{ENV}/yologram.auth.jwt.secret
      expire: 86400
      issuer: yologram.link
      audience: yologram.client
    admin-jwt:
      secret:   # /yologram/service/yologram-api-v1_{ENV}/yologram.auth.admin-jwt.secret
      expire: 86400
      issuer: yologram.link
      audience: yologram.admin

springdoc:
  swagger-ui:
    path: /api/v1/docs
    tags-sorter: alpha
  api-docs:
    path: /api/v1/api-docs

# Lettuce는 Hikari 같은 연결 INFO 로그가 없고 연결 성공 메시지가 DEBUG뿐 — 이 클래스만 열어
# Redis(Valkey) 연결 여부를 로그로 확인한다 ("Connecting to Redis at {}: Success" / 실패 시 원인).
# 연결 수립·재연결 시점에만 찍혀 볼륨이 작다(명령 단위 로그인 io.lettuce.core 전체 DEBUG는 금지).
# 연결은 lazy라 부팅 시점이 아니라 첫 캐시 연산 때 나온다. 재연결·중단은 ConnectionWatchdog가 기본 INFO/WARN으로 남김
logging:
  level:
    io.lettuce.core.AbstractRedisClient: DEBUG

management:
  # 캐시는 부가 기능 — Redis 장애가 서비스 health DOWN으로 전파되지 않도록 Redis health indicator 비활성
  # (Redis 자동구성 exclude와 별개로 actuator RedisHealthContributorAutoConfiguration이 수동 빈으로 활성화됨)
  health:
    redis:
      enabled: false
  tracing:
    sampling:
      probability: 1.0
  otlp:
    tracing:
      endpoint:   # /yologram/service/yologram-api-v1_{ENV}/management.otlp.tracing.endpoint
      headers:
        Authorization:  # /yologram/service/yologram-api-v1_{ENV}/management.otlp.tracing.headers.Authorization
    metrics:
      export:
        url:    # /yologram/service/yologram-api-v1_{ENV}/management.otlp.metrics.export.url
        step: 1m
        headers:
          Authorization:  # /yologram/service/yologram-api-v1_{ENV}/management.otlp.metrics.export.headers.Authorization
    logging:
      endpoint:   # /yologram/service/yologram-api-v1_{ENV}/management.otlp.logging.endpoint
      headers:
        Authorization:  # /yologram/service/yologram-api-v1_{ENV}/management.otlp.logging.headers.Authorization
  opentelemetry:
    resource-attributes:
      service.name: ${spring.application.name}
      deployment.environment.name: ${spring.profiles.active:default}
      service.instance.id: ${server.hostname:unknown}
      service.namespace: yologram
  metrics:
    tags:
      service.name: ${spring.application.name}
      deployment.environment.name: ${spring.profiles.active:default}
      service.instance.id: ${server.hostname:unknown}
      service.namespace: yologram
