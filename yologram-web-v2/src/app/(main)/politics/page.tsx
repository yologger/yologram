// 정치 준비 중: layout이 ComingSoon으로 가로채므로 이 page는 렌더되지 않는다.
// 구현 시작 시 아래 redirect를 복구한다.
export default function PoliticsPage() {
  return null
}

// import { redirect } from 'next/navigation'
//
// export default function PoliticsPage() {
//   redirect('/politics/news')
// }
